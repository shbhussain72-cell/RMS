# LSD gaps — committed baseline

Generated by `node scripts/audit-lsd.mjs --write`. Do not hand-edit — rerun the script.

This file is the reference point for every later remediation session: the numbers below
are the "before". A session is judged by the delta against them, so regenerate it only
when you intend to move the baseline.

Source: `src/i18n/lsd.json`, generated from `RMS_Mumineen_LSD_wordlist_v4.xlsx` via
`npm run build:lsd`. Neither file is modified by the audit.

## Summary

| Class | Rows | % of dictionary |
|---|---:|---:|
| Total entries | 1182 | 100.0% |
| Corrupted English keys | 2 | 0.2% |
| Class A — byte damage | 0 | 0.0% |
| Class B — Kanz input | 0 | 0.0% |
| Empty values | 19 | 1.6% |
| Identity pass-throughs | 7 | 0.6% |
| **Distinct defective rows** | **26** | **2.2%** |
| **Usable rows** | **1156** | **97.8%** |

Classes overlap — a corrupted key can also hold a damaged value — so the counts
sum to more than the distinct-defective total.

### What happened to the old "mojibake" figure

This audit used to report one class called *mojibake*, counted by a local six-pair
regex, and it stood at **190 rows** — reported as unfixable, because that one name
carried one verdict. It was two different problems:

- **Class A** is real byte damage. Bytes are gone; the row must be re-keyed by hand.
- **Class B** is Kanz al-Lulu keyboard output. Nothing was lost — it converts exactly,
  and `src/i18n/kanzNorm.mjs` now converts it at the generator, the editor and the
  sync, so it never reaches this file. **No action needed.**

**Every row it flagged was class B.** Reclassified against the pre-repair workbook:

| | rows |
|---|---:|
| Flagged by the old six-pair regex | 234 |
| → class A only (must be re-keyed by hand) | **0** |
| → class B only (converts exactly) | **234** |
| → both | 0 |
| → neither | 0 |

So the headline number is not "190 rows to re-key" but **0**. Not one value in this
wordlist has ever had byte damage — class A across the whole corpus is 0. Every row the
old figure counted was recoverable, and is now recovered on the way into the dictionary.

The old regex was also wrong in both directions: it listed `صص`, which occurs **0**
times in this corpus, and omitted `كك` and `حح`, which cost it **20** class B rows it
never reported. The set is now the seven pairs proved against the corpus in
`docs/kanz-digraphs.md`, held in one place.

Class B reading **0** here is the assertion, not a formality: this file is generated
from `lsd.json`, so a non-zero count means an entry path stopped normalising.

The wordlist itself is a separate question — 189 rows still hold Kanz input at source,
awaiting the rulings in `docs/kanz-unattested.md`. That is by design: the spreadsheet
is the owner's, and the repair only rewrote what the corpus could vouch for.

## 1. Corrupted English keys (2)

The English column contains Arabic-script characters. The app looks up by English
string, so these rows can never match anything on screen: the translation is stranded
and the English it covered renders untranslated. A find/replace wrote the LSD value
into the key column.

**Not repaired here.** The original English has to be recovered from the design source.

Two kinds, same consequence:

- `arabic-in-key` — a find/replace wrote the LSD value into the English column.
- `invisible-mark` — a zero-width bidi control (U+200F and friends) rode along with a
  copy/paste out of an RTL cell. Invisible in Excel; makes the key unmatchable.

| Page | Kind | Key as stored | Marks | Latin remnant |
|---|---|---|---|---|
| — | invisible-mark | `‏Home` | U+200F | Home |
| — | invisible-mark | `‏Registered` | U+200F | Registered |

## 2a. Class A — byte damage (0)

UTF-8 read as latin-1, a U+FFFD replacement character, or a lone surrogate. Detected by
`detectByteDamage` in `src/dev/mojibake.ts`.

**These cannot be fixed programmatically.** The bytes are gone — a guess that looks
plausible is worse than a rejection, because it lands in the wordlist as if it were
authored. Each row must be re-keyed by hand. Page numbers are included for that.

None. No value in the dictionary carries byte damage.

## 2b. Class B — Kanz keyboard input (0)

The seven doubled pairs ظظ ثث سس كك حح ضض طط — Kanz al-Lulu keyboard output, where each
Urdu-specific letter arrives as a doubled Arabic one.

**No action needed, and a non-zero count here is a regression.** Nothing was lost in
this encoding and it converts exactly; `src/i18n/kanzNorm.mjs` does so at the generator,
the editor and the sync. This file is generated from `lsd.json`, downstream of that
conversion, so anything listed below means an entry path stopped normalising.

None — as expected. The conversion is working.

## 3. Empty values (19)

Row exists, LSD cell blank. Renders English.

| Page | English key |
|---|---|
| — | (Demo) Tap to advance the demo. |
| — | 4.0 MB |
| — | Choose the Host City radio for each member in the table below. |
| — | Choose the Host City radio for each member in the table. |
| — | Choose the Relay City radio, then pick a city from the dropdown for each member. |
| 1 | Enter your 8-digit ITS ID |
| 1 | Enter your password |
| — | PDF · 248 KB |
| 1 | Password |
| 1 | Remember Me |
| — | Stuttgart |
| — | System |
| — | United Arab Emirates |
| — | Vancouver |
| 73 | Your slot is temporarily held while the timer runs. Tap Confirm before it expires to compl… |
| — | added |
| — | until they accept their invitation. A seat is only used when each one accepts. |
| — | ‏Home |
| — | ‏Registered |

## 3b. Sentinel rows — awaiting a decision (5)

These cells hold an INSTRUCTION, not a translation. The wordlist owner wrote a keyword
where the LSD text goes; nobody has yet decided what the string should say, so **the app
shows English** in the meantime. They are listed apart from the empty rows above because
the action needed differs: an empty row needs someone to translate it, a sentinel row
needs someone to say what was meant.

| Page | English key | Cell contains |
|---|---|---|
| — | Children below 10 years of age must be assigned to a parent or guardian before p… | `remove` |
| — | Host City registrations may close early once capacity is reached. | `remove` |
| — | Registration closes on 5 June 2026 at 11:59 PM. Complete all group registrations… | `remove` |
| — | Tickets will only be generated after all required approvals are completed. | `remove` |
| — | Venue allocation is subject to availability and capacity limits. | `remove` |

## 4. Identity pass-throughs (7)

LSD value equals the English key (compared with zero-width direction marks stripped,
since the build step prefixes U+200F to mixed-script values).

Some are deliberate loanwords — `PDF`, `zone`, `ITS` — and some are untranslated
placeholders. This script does not try to tell them apart; that is the wordlist
owner's decision.

| Page | English key = LSD value |
|---|---|
| persistent | Home |
| Login | ITS id |
| 2 (22 pages) | Registered |
| 19 (2 pages) | Soft Diet |
| 15 (2 pages) | Tap to upload visa (PDF) |
| countdown | d |
| countdown | h |

## 4b. Identity by policy — no action needed (9)

These also read the same in both languages, and that is **correct**. They are acronyms,
units, format masks and agreed loanwords — see `docs/loanword-policy.md`. They are listed
so it is clear they were considered, not overlooked.

| English key | Why identity is right |
|---|---|
| 248 KB | File size: digits plus an SI unit. Neither half is language. |
| AM | Clock meridiem. Rendered inside an LTR isolate by formatTime(). |
| IST | Timezone acronym; unambiguous only in its Latin form. |
| ITS | System name. ITS is the identity platform itself, not a describable noun. |
| ITS ID | System name + universally-used field label. |
| PDF | File-format acronym; printed on the file itself. |
| PM | Clock meridiem. Rendered inside an LTR isolate by formatTime(). |
| Registration | Loanword (see loanwords.words). Currently stored lower-cased, which is why it reads as an identity pass-through rather than a translation. |
| mm/dd/yyyy | Date-format mask — an instruction about literal characters to type. |

## 5. DOM scanner baseline (A/B/C)

Produced by the CoveragePanel DOM scanner, which walks rendered text nodes while the
app is in LSD and flags any node containing a Latin word with no Arabic character.
This finds hardcoded JSX literals that never call `t()` — invisible to the old
miss-counting panel, which is why it reported only ~60.

| Class | Meaning | Fix owner |
|---|---|---|
| **A** | Key exists in the dictionary WITH a real value — translation exists, the string just is not wired | developer |
| **B** | Key exists but the value is empty or an identity pass-through | wordlist owner |
| **C** | No dictionary key at all | wordlist owner, then developer |

Baseline is captured by walking the app in LSD and using the panel's **Export JSON**
button, then committing the totals here. See `artifacts/audit/` for the matching
screenshots.

<!-- SCANNER_BASELINE_START -->
_Not yet captured. Run the app in LSD, walk the routes, export from the panel._
<!-- SCANNER_BASELINE_END -->

